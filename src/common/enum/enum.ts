export enum USER_TYPE {
  ADMIN = 'admin',
  PARTNER = 'partner',
}

export enum PUSH_PURPOSE {
  AD = 'ad',
  INFO = 'info',
}

export enum MEMBER_TARGET {
  ALL = 'all',
  MEMBER = 'member',
  GROUP = 'group',
}

export enum OS_Type {
  ALL = 'ALL',
  ANDROID = 'ANDROID',
  IOS = 'IOS',
}

export enum SCHEDULE_TYPE {
  IMMEDIATELY = 'immediately',
  RESERVE = 'reserve',
  BATCH = 'batch',
}
